{
  "name": "lavamoat",
  "private": true,
  "devDependencies": {
    "ava": "^4.0.1",
    "depcheck": "^1.4.3",
    "eslint": "^7.32.0",
    "eslint-plugin-ava": "^11.0.0",
    "eslint-plugin-standard": "^5.0.0",
    "tap-spec": "^5.0.0"
  },
  "engines": {
    "node": ">=12.16.1"
  },
  "scripts": {
    "test": "pnpm run test --recursive --if-present",
    "test:prep": "pnpm run test:prep --recursive --if-present",
    "lint": "pnpm run lint --recursive --if-present",
    "lint:fix": "pnpm run lint:fix --recursive --if-present",
    "build": "pnpm run build --recursive --if-present",
    "setup": "pnpm run allow-scripts --recursive --if-present"
  },
  "workspaces": [
    "packages/*"
  ],
  "version": "0.0.0"
}
