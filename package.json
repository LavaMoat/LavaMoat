{
  "name": "lavamoat",
  "private": true,
  "devDependencies": {
    "ava": "^4.0.1",
    "depcheck": "^1.4.0",
    "eslint": "^7.22.0",
    "eslint-plugin-ava": "^11.0.0",
    "eslint-plugin-standard": "^5.0.0",
    "tap-spec": "^5.0.0"
  },
  "engines": {
    "node": ">=12.16.1"
  },
  "scripts": {
    "test": "npm run --workspaces --if-present test",
    "test:prep": "npm run --workspaces --if-present test:prep",
    "lint": "npm run --workspaces --if-present lint",
    "lint:fix": "npm run --workspaces --if-present lint:fix",
    "build": "npm run --workspaces --if-present build",
    "publish": "npm publish --workspaces --if-present",
    "setup": "npm run allow-scripts --workspaces --if-present"
  },
  "workspaces": [
    "packages/*"
  ]
}
