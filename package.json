{
  "name": "lavamoat",
  "private": true,
  "devDependencies": {
    "ava": "^4.0.1",
    "depcheck": "^1.4.0",
    "eslint": "^7.22.0",
    "eslint-plugin-ava": "^11.0.0",
    "eslint-plugin-standard": "^5.0.0",
    "tap-spec": "^5.0.0",
    "turbo": "^1.1.2"
  },
  "engines": {
    "node": ">=12.16.1"
  },
  "scripts": {
    "test": "turbo run test",
    "test:prep": "turbo run test:prep",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "build": "turbo run build",
    "setup": "turbo run allow-scripts"
  },
  "workspaces": [
    "packages/*"
  ],
  "version": "0.0.0"
}
