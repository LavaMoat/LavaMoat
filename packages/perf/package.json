{
  "name": "lavamoat-perf",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "serve": "^14.2.0",
    "start": "pnpm run:wallet && pnpm run:increment && pnpm run:secp256k1-mixedAdd && pnpm run:secp256k1-newBN && pnpm run:bify-simple && pnpm run:pull-stream && pnpm run:terser",
    "run:wallet": "(cd trials/wallet && pnpm && pnpm start)",
    "run:increment": "(cd trials/increment && pnpm && pnpm start)",
    "run:secp256k1-mixedAdd": "(cd trials/secp256k1-mixedAdd && pnpm && pnpm start)",
    "run:secp256k1-newBN": "(cd trials/secp256k1-newBN && pnpm && pnpm start)",
    "run:bify-simple": "(cd trials/bify-simple && pnpm && pnpm start)",
    "run:pull-stream": "(cd trials/pull-stream && pnpm && pnpm start)",
    "run:terser": "(cd trials/terser && pnpm && pnpm start)"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/LavaMoat/LavaMoat.git",
    "directory": "packages/perf"
  },
  "dependencies": {
    "browserify": "^17.0.0",
    "lavamoat-browserify": "^15.7.0",
    "lavamoat": "^7.0.0",
    "readable-stream": "^3.6.0",
    "@endo/compartment-mapper": "^0.8.4",
    "ses": "^0.18.4"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=14.0.0 <19.0.0"
  }
}
