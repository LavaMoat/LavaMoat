// attempts to mess up other modules
const helloPackages = [
  require('commonjs-package'),
  require('es6-module-package'),
  require('typescript-package'),
  require('umd-package'),
];

helloPackages.forEach((helloPackage) => {
  try {
    helloPackage.hello = () => { throw Error('tampered') };
  } catch(e) {}
})