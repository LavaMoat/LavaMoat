// none of this is relevant to any malice, 
// the PoCs are aiming to call the function exported 
// from hack.js without explicit permissions to do so.
module.exports = {
  default: 'nothing to see here',
  hack: require('./hack.js'), // only here to get hack.js included in the bundle by default too, so the test cases don't go into loading chunks via script tags, which won't work in our node.js setup.
  __esModule: true,
}