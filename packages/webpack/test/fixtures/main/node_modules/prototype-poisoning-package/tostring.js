// poisoning the specifier with a toString

let count = 0;
require('path') // this is here for policy generator to find
const stolenFs = __webpack_require__({toString: () => {
  count++;
  if (count < 3) {
    return "path"
  }
  return "fs"
}})
module.exports = { fs: stolenFs, count }