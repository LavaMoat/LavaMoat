// An anonymous default export function declaration. webpack >=5.107 emits a
// `__webpack_require__.dn(...)` call for these to set `.name` per ES spec.
export default function () {
  return 'called'
}
