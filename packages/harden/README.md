# @lavamoat/harden

A tool to provide reasonable defaults and hardening to JavaScript projects.

- Generates a config for your package manager to prevent install-time dangers.
- Helps generate and validate the list of allowed lifecycle scripts from your dependencies.
- Helps configure increasingly strong mitigations for your runtime
