# Snapshot report for `test/idempotency.spec.js`

The actual snapshot is saved in `idempotency.spec.js.snap`.

Generated by [AVA](https://avajs.dev).

## hardenDefaults - npm - idempotent (paranoid applied twice)

> log

    [
      'All opinions are already fully applied. No hardening should be needed.',
      'Found git dependencies in package.json. Blocking them by setting allow-git to none. Consider replacing them with safer sources if needed.',
      'Found git dependencies in package.json. They won\'t work with git disabled and you chose paranoid level. Consider removing them or moving to a safer source if possible.',
    ]

## hardenDefaults - yarn - idempotent (paranoid applied twice)

> log

    [
      'Found git dependencies in package.json. Edit approvedGitRepositories in .yarnrc.yml accordingly.',
      'Recommended follow-up command: yarn install',
      'Recommended follow-up command: yarn allow-scripts auto',
    ]

## hardenDefaults - pnpm - idempotent (paranoid applied twice)

> log

    [
      'No node_modules found. Skipping lifecycle script allowlist generation.',
      'Recommended follow-up command: pnpm approve-scripts',
    ]
