{
  "name": "test-4",
  "scripts": {
    "setup": "yarn install && yarn setup:fake-deps",
    "setup:fake-deps": "cp -R fake_node_modules/* ./node_modules/"
  },
  "dependencies": {
    "events": "^3.2.0"
  }
}
