const a = 5;
const T = 10;
const b = 2;

try {
    // This is valid JavaScript that becomes invalid after type stripping
    /* @ts-ignore */
    const result = a<T>(b);
} catch (e) {
    throw Error('Type stripping should not be applied to this file', { cause: e });
}

// make policy snapshot fail if this file goes missing
process.env.SHOULD_BE_INCLUDED_IN_POLICY = 'lol';

